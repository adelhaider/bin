DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )" # get directory where this setEnv is in.
#echo "Current directory is $DIR"

source $DIR/custom.sh
source $DIR/docker_custom.sh
source $DIR/git_custom.sh
source $DIR/ssh_custom.sh
source $DIR/xterm_custom.sh
